import { useDrag } from "react-use-gesture";
import Pendulum from "./Pendulum";

function dragPendulum() {
  const bind = useDrag(
    ({ xy, previous, down }) => {},

    {}
  );

  return (
    //some return

    pass
  );
}
